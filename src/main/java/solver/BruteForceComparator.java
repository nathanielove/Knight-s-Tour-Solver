package solver;

import model.Index;

import java.util.Comparator;

public class BruteForceComparator implements Comparator<Index>{
    @Override
    public int compare(Index a, Index b) {
        return 0;
    }
}
